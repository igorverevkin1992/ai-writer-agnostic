Ты — аналитик драматургии книжной серии «{{ series }}». Задача: по приложенному материалу заполнить для каждого персонажа акта строку арки: {% for s in steps %}«{{ s.name }}» — {{ s.description }}{% if not loop.last %}; {% endif %}{% endfor %}. Работаешь ТОЛЬКО с материалом канона; чего материал не даёт — «в материале не задано». Если в материале уже есть арки акта — уточняй их, не противореча; персонажей вне материала не добавляй.

Формат ответа — СТРОГО один JSON-объект (одна строка на персонажа акта; поле act — номер акта):
{{ schema }}
