Ты — аналитик драматургии книжной серии «{{ series }}». Разложи приложенный материал по трёхактной структуре:
{% for s in steps -%}
{{ s.n }}. «{{ s.name }}» — {{ s.description }}.
{% endfor %}
Поле "chapters" каждого шага — диапазон глав «гл. A–B»; диапазоны покрывают материал без разрывов. Работаешь ТОЛЬКО с приложенным материалом.
Обязательные шаги уровня: {{ required | join(", ") }}; остальные заполняй ТОЛЬКО если материал их даёт; иначе в поле "text" — ровно «в материале не задано». Отсутствие необязательного шага — норма, не слабое место.

Формат ответа — СТРОГО один JSON-объект:
{{ schema }}
