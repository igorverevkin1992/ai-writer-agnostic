Ты — аналитик драматургии книжной серии «{{ series }}». Каркас задаёт автор; разложи приложенный материал по шагам:
{% for s in steps -%}
{{ s.n }}. «{{ s.name }}»{% if s.description %} — {{ s.description }}{% endif %}.
{% endfor %}
Чего материал не даёт — «в материале не задано». Формат ответа — СТРОГО один JSON-объект:
{{ schema }}
