"""Тесты профиля эталона (УГАР).

`test_миграция.py` — приёмочный тест универсальности (FR-MG-1…FR-MG-4): выполняется, когда библиотека эталона
доступна (переменная окружения KONVEYER_ETALON = путь к папке библиотеки; либо соседний клон
`../igorverevkin1992/ugar-library`). Без библиотеки тесты пропускаются."""
